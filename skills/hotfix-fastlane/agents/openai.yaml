interface:
  display_name: "Hotfix Fastlane"
  short_description: "Go from incident signal to validated hotfix and report"
  default_prompt: "Use $hotfix-fastlane to handle a production hotfix from logs through patch, verification, and Jira-ready reporting."
policy:
  allow_implicit_invocation: true
