interface:
  display_name: "Code Review"
  short_description: "Find bugs, regressions, and risk in changed code"
  default_prompt: "Use $code-review to review a diff and report the highest-severity bugs, regressions, and testing gaps first."
policy:
  allow_implicit_invocation: true
